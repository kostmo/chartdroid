<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.googlecode.chartdroid.market.sales"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:icon="@drawable/icon" android:label="@string/app_name">
    
    
        <!-- The following are DEMO providers that you should place in your own apps. -->
      	<provider android:label="Some random data"
   			android:name="com.googlecode.chartdroid.market.sales.provider.SalesContentProvider"
	        android:authorities="com.kostmo.market.provider.sales"
		/>
		
    
    
        <activity android:name=".Main"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
    <uses-sdk android:minSdkVersion="4"/>



<uses-permission android:name="android.permission.INTERNET"></uses-permission>

</manifest> 